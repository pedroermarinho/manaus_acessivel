import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:manausacessivel/app/app_module.dart';

void main() {
  initModule(AppModule());
  // MysqlController mysql;
  //
  setUp(() {
    //     mysql = AppModule.to.get<MysqlController>();
  });

  group('MysqlController Test', () {
    //   test("First Test", () {
    //     expect(mysql, isInstanceOf<MysqlController>());
    //   });

    //   test("Set Value", () {
    //     expect(mysql.value, equals(0));
    //     mysql.increment();
    //     expect(mysql.value, equals(1));
    //   });
  });
}
