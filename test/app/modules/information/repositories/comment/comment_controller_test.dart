import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:manausacessivel/app/app_module.dart';

void main() {
  initModule(AppModule());
  // CommentController comment;
  //
  setUp(() {
    //     comment = AppModule.to.get<CommentController>();
  });

  group('CommentController Test', () {
    //   test("First Test", () {
    //     expect(comment, isInstanceOf<CommentController>());
    //   });

    //   test("Set Value", () {
    //     expect(comment.value, equals(0));
    //     comment.increment();
    //     expect(comment.value, equals(1));
    //   });
  });
}
